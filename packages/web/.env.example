# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key  # For webhooks (keep secret!)

# Google Maps
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID=your-map-id  # Create at https://console.cloud.google.com/google/maps-apis/studio/maps

# Stripe
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs
# Create these products in Stripe Dashboard:
# 1. "LatticeForge Starter" - $49/month
# 2. "LatticeForge Pro" - $199/month
# 3. "LatticeForge Enterprise" - $499/month
STRIPE_PRICE_STARTER=price_...
STRIPE_PRICE_PRO=price_...
STRIPE_PRICE_ENTERPRISE=price_...

# =============================================================================
# SELF-HOSTED LLM ENSEMBLE (RunPod Serverless)
# =============================================================================
# LatticeForge uses a two-tier self-hosted LLM architecture:
#
# TIER 1: WORKHORSE (Qwen2.5-7B-AWQ)
#   - Simple tasks: JSON extraction, scoring, classification, validation
#   - Cost: ~$0.0003/request
#   - GPU: Single A40/L40 (~$0.39/hr)
#
# TIER 2: ELLE (Elle-72B-Ultimate)
#   - Complex tasks: Intel briefings, user chat, analysis, training gen
#   - Cost: ~$0.001/request
#   - GPU: 2x H100 80GB (~$7.98/hr)
#
# The ModelRouter automatically routes requests to the appropriate tier.
# =============================================================================

# RunPod API Key (shared across endpoints)
RUNPOD_API_KEY=your-runpod-api-key

# WORKHORSE TIER - Simple tasks (optional, falls back to Elle if not set)
# Deploy Qwen2.5-7B-Instruct-AWQ on a single A40/L40
WORKHORSE_ENDPOINT=https://api.runpod.ai/v2/YOUR_WORKHORSE_ENDPOINT
WORKHORSE_MODEL=Qwen/Qwen2.5-7B-Instruct-AWQ
WORKHORSE_API_KEY=your-runpod-api-key  # Or use RUNPOD_API_KEY

# ELLE TIER - Complex analysis, user-facing
# Deploy aphoticshaman/elle-72b-ultimate on 2x H100 80GB
LFBM_ENDPOINT=https://api.runpod.ai/v2/YOUR_ELLE_ENDPOINT
LFBM_MODEL=aphoticshaman/elle-72b-ultimate
LFBM_API_KEY=your-runpod-api-key  # Or use RUNPOD_API_KEY

# Production control
LF_PROD_ENABLE=true  # Set to true in production to enable LLM calls
PREFER_LFBM=true     # Always prefer self-hosted over external APIs

# =============================================================================
# GUARDIAN SECURITY LAYER (Upstash Redis)
# =============================================================================
# Rate limiting and caching via Upstash Redis
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# =============================================================================
# WEB SEARCH (for Elle Study Book)
# =============================================================================
# Priority: SearXNG (free) → Serper (paid) → Brave (paid) → DuckDuckGo (free/limited)
#
# SearXNG - Self-hosted meta-search (FREE, unlimited)
# Deploy: docker run -d -p 8080:8080 searxng/searxng
# Or use public instance: https://searx.be, https://search.sapti.me
SEARXNG_URL=http://localhost:8080

# Serper.dev - Google Search API ($50/100k searches)
SERPER_API_KEY=your-serper-api-key

# Brave Search API (2k free/month, then $5/20k)
BRAVE_SEARCH_API_KEY=your-brave-api-key

# =============================================================================
# CRON & INTERNAL SERVICES
# =============================================================================
CRON_SECRET=random-32-char-string
INTERNAL_SERVICE_SECRET=random-32-char-string

# =============================================================================
# EMAIL (Resend)
# =============================================================================
RESEND_API_KEY=re_...
