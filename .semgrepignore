# Semgrep ignore patterns
# https://semgrep.dev/docs/ignoring-files-folders-code

# PackageBuilder.tsx export functions use escapeHtml() to sanitize all dynamic content
# before template insertion. The XSS findings are false positives because Semgrep
# cannot statically analyze that escapeHtml() properly sanitizes the input.
#
# The escapeHtml function escapes: & < > " ' /
# All dynamic values (content.title, presetInfo.name, content.sections, etc.)
# are passed through escapeHtml() before HTML template insertion.
#
# See: packages/web/components/intelligence/PackageBuilder.tsx:301-308
packages/web/components/intelligence/PackageBuilder.tsx
